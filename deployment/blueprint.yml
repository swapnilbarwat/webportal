name: webportal:0.0.3
clusters:
  webportal:
    services:
      breed: 
        name: webportal
        deployable: harshals/webportal:0.0.3
        ports:
          app_port: 80/tcp
      scale:
        cpu: 0.2
        memory: 64MB
        instances: 1